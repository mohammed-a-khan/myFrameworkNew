<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender configuration -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File appender configuration -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>target/test.log</file>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Disable Apache HttpClient wire logging (raw HTTP data) -->
    <logger name="org.apache.hc.client5.http.wire" level="ERROR"/>
    
    <!-- Disable Apache HttpClient headers logging -->
    <logger name="org.apache.hc.client5.http.headers" level="ERROR"/>
    
    <!-- Set Apache HttpClient general logging to INFO -->
    <logger name="org.apache.hc.client5" level="INFO"/>
    <logger name="org.apache.hc.core5" level="INFO"/>
    
    <!-- Selenium logging -->
    <logger name="org.openqa.selenium" level="INFO"/>
    
    <!-- TestNG logging -->
    <logger name="org.testng" level="INFO"/>
    
    <!-- Your application logging -->
    <logger name="com.testforge" level="DEBUG"/>
    <logger name="com.orangehrm" level="DEBUG"/>
    
    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>